image: python:3.10-slim

default:
  tags:
    - test
    
before_script:
  - python3 -m venv venv
  - . venv/bin/activate && pip install -r requirements.txt

run:
  script:
    - . venv/bin/activate && python check_internet_speed.py


