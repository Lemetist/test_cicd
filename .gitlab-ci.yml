stages:
  - test
  - build
  - prod

install_image:
  stage: test
  tags:
    - test
  script:
    - echo "install_iag"
    - ls
    - whoami

build_image:
  stage: build
  tags:
    - test
  script: 
    - echo "build image"

prod_installer:
  stage: prod
  tags:
    - test
  script:
  - ls
